{
  "name": "profsis3-sed-app",
  "version": "0.1.0",
  "private": true,
  "description": "App Android (kiosk) que reproduz a extensao ProfSis3 dentro do portal Sala do Futuro (SED), usando Capacitor apenas como framework de build/plugins.",
  "scripts": {
    "sync-sources": "node scripts/sync-extension-sources.mjs",
    "build-bundles": "node scripts/build-bundles.mjs",
    "prebuild": "npm run sync-sources && npm run build-bundles",
    "cap:sync": "npm run prebuild && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
